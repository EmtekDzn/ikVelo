{% extends 'BackOfficeBundle:Default:backtemplate.html.twig' %}

{% block body %}
    <h1>Deplacement edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Editer" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('deplacements_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
